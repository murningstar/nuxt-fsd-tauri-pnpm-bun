# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Bun's "bun.lock" is generated only on build step. See property `beforeBuildCommand` in `src-tauri/tauri.conf.json`
# bun.lock
# Bun is not ready to be used in nuxt monorepo - currently it's not possible to use it's API in dev mode. It can only be used as another folder of monorepo.    
# https://claude.ai/public/artifacts/a388de11-5cad-4c06-b332-1a0c13aefa1b